"""{{ title or 'Service' }} main application."""

from fastapi import FastAPI

app = FastAPI(title="{{ title or 'Service' }}")


@app.get("/")
async def root():
    """Root endpoint."""
    return {"message": "{{ title or 'Service' }} is running"}


@app.get("/health")
async def health():
    """Health check endpoint."""
    return {"status": "healthy"}
