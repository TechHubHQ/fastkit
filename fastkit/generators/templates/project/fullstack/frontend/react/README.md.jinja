# {{ project_name }} Frontend

This is the frontend application for {{ project_name }}, built with React, TypeScript, and {{ build_tool }}.

## Features

- ⚡️ React 18 with TypeScript
- 🚀 {{ build_tool }} for fast development and building
- 🎨 Tailwind CSS for styling
- 📱 Responsive design
- 🔒 {% if has_auth %}Authentication system with JWT{% else %}Ready for authentication integration{% endif %}
- 🌐 API integration with the FastAPI backend

## Getting Started

### Prerequisites

- Node.js 18+ and npm/yarn
- The backend API running on {{ backend_url }}

### Installation

1. Install dependencies:
```bash
npm install
```

2. Start the development server:
```bash
npm run dev
```

3. Open [http://localhost:3000](http://localhost:3000) in your browser

## Available Scripts

- `npm run dev` - Start development server
- `npm run build` - Build for production
- `npm run preview` - Preview production build
- `npm run lint` - Run ESLint

## Project Structure

```
src/
├── components/     # Reusable UI components
├── pages/         # Page components
├── services/      # API services
├── types/         # TypeScript type definitions
├── utils/         # Utility functions
├── styles/        # Global styles
{% if has_auth %}├── auth/          # Authentication related components{% endif %}
└── App.tsx        # Main application component
```

## API Integration

The frontend communicates with the FastAPI backend through:
- Base URL: {{ backend_url }}
- API endpoints: {{ backend_url }}/api/v1/
{% if has_auth %}
## Authentication

The app includes a complete authentication system:
- Login/logout functionality
- Protected routes
- JWT token management
- Automatic token refresh
{% endif %}

## Building for Production

```bash
npm run build
```

The built files will be in the `dist/` directory, ready for deployment.

## Environment Variables

Create a `.env` file in the root directory:

```env
VITE_API_BASE_URL={{ backend_url }}
```